# stiffGWpy fast v0.2 DN_gw and Runtime Optimization Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development for behavior changes and superpowers:verification-before-completion before any completion claim.

**Goal:** Starting from remote `fast_v0.2` HEAD `767056d`, reduce DN_gw error without sacrificing the current spectrum accuracy, while reaching a stable warm runtime of 4 ms/point or better where evidence supports it.

**Architecture:** Keep the existing physical guards, fallback behavior, output contract, and independent DOP853 reference unchanged. First use fixed-environment profiling and component A/B probes; then implement only candidates whose measured error contribution and runtime tradeoff are favorable. Keep science/native output nodes separate from any integration reconstruction only if the candidate passes the parameter-space matrix.

**Tech Stack:** Python 3.11, NumPy, SciPy, Numba `workqueue`, pytest, existing `stiffgwpy_fast.reference` oracle, JSON benchmark artifacts.

**Spec:** `C:\Users\联想\.codex\attachments\8374d031-7794-437a-8092-1bf8423b4f4a\pasted-text-1.txt`

## Global Constraints

- Do not infer current hotspots from old profiler output; fresh HEAD measurements are required.
- Preserve physical guard/fallback semantics, float64 output, native `eval_freqs`, and no numerical failures.
- Do not repeat rejected blind node increases, smaller-step-only, generic RK4/Magnus, phase-only, tail-only, or thread/chunk micro-tuning experiments without new evidence.
- Treat old benchmark numbers as historical context only; all release tables must be regenerated from the final release HEAD.
- GitHub commits, if authorized later, must use `2966684515@qq.com` and the format `English: 中文内容`.

### Task 1: Fresh baseline and stage profiler

**Files:**
- Create: `scripts/run_fixed_profile.py`
- Modify: `scripts/profile_fast_breakdown.py`
- Output: `docs/profile_head_t*.json`, `docs/bench_head_t20.json`

- [x] Fix Numba `workqueue`, CPU affinity, warmup/cold-vs-warm convention, and thread counts `1/2/4/8/16/20/32`.
- [x] Record background, goal construction, frequency preparation, `fast_phi_s2_split`, kernel, quadrature, outer and residual output/assembly time.
- [x] Run reuse enabled/disabled A/B and record kernel-call count and output digests.

### Task 2: DN_gw error-budget isolation

**Files:**
- Create: `scripts/error_budget_probe.py`
- Output: `docs/error_budget_probe_head.json`, `docs/error_budget_probe_*.json`

- [x] Compare fast smooth primitive against exact continuous primitive with all other settings fixed.
- [x] Compare outer reuse enabled/disabled with all other settings fixed.
- [x] Compare solver Simpson result with high-accuracy PCHIP/adaptive integration of the identical spectrum.
- [ ] Add same-grid independent-reference transfer/tail comparison and quantify the remaining background, transfer/tail, frequency reconstruction, quadrature, outer, and oracle-floor terms. (Default full-reference scalar comparison is complete; same-grid spectrum/transfer isolation remains.)

### Task 3: Embedded frequency integration candidate

**Files:**
- Modify: `stiffgwpy_fast/fast_sgwb.py`
- Modify: `stiffgwpy_fast/reference.py` only if a shared tested integration helper is justified.
- Test: `tests/test_fast_sgwb.py`, `tests/test_reference.py` or a focused new test module.

- [ ] Write failing tests for an embedded coarse/fine integral estimate and preservation of native likelihood nodes.
- [x] Implement the smallest opt-in PCHIP reconstruction candidate that integrates solved native nodes without changing spectrum output.
- [ ] Measure default, low-T, high-T, stiff, and parameter-space samples against the independent reference; reject if low-T bias or p95 worsens.
- [ ] Add runtime and node-count records to the Pareto benchmark.

### Task 4: Observable-aware outer reuse and background reconstruction

**Files:**
- Modify only after Task 3 evidence identifies a need: `stiffgwpy_fast/fast_sgwb.py`, `stiffgwpy_fast/exact_background.py`.
- Test: focused reuse/primitive tests.

- [ ] Derive an observable-aware reuse estimate from transfer/spectrum/DN telemetry rather than a blind threshold.
- [ ] Test quadratic/cubic smooth primitive reconstruction without crossing the reheating kink and with `h=.01/.005/.0025` convergence.
- [ ] Accept only if DN improves materially with less than 5% runtime overhead and no false-safe reuse.

### Task 5: Final validation and release artifacts

**Files:**
- Modify: `docs/validation/validation_manifest.json`, benchmark JSON/MD, `README.md`, `README_zh.md`, `CHANGELOG.md`.

- [ ] Run full regression, guards/fallback/NaN checks, native likelihood-node checks, and parameter-space oracle matrix.
- [ ] Produce runtime/accuracy Pareto points for old-main fast, current v0.2, and each accepted candidate.
- [ ] Regenerate all release artifacts from the exact final HEAD; do not reuse historical numbers.
