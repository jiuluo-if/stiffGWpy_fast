# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtualenvs / build
.venv/
venv/
dist/
build/

# stiffgwpy local MCMC scaffolds (regenerated by the MCMC drivers; not source)
docs/mcmc_smoke/
docs/mcmc/chains/

# --- Local SageNet+ comparison (regenerated by sagenet_compare/run_compare.py) ---
# The comparison data artifacts are READ-ONLY replays: keep the script and the
# report (research material) but never re-commit the generated JSON spectra.
/sagenet_compare/data/

# Local SageNet source clone, its bundled PyTorch weights, and the dedicated
# compare venv (numpy<2 + torch 2.3.1 + astropy 6) used to run the emulator.
# These are machine-local reproduction aids, not repository content.
/sagenetgw/
/sagenet_env/
*.pth
