# EPTA DR2 mock 数据资源

[English](README.md) | 中文说明

`../IPTA.yaml` 使用本目录中的资源：

- `EPTA_dr2new_mock.dat`：`IPTA` 适配器读取的样本值。
- `freqs_dr2new.txt`：对应的频率 bin，每行一个值。

适配器使用 YAML 中的 `Nfreqs` 选择参与计算的频点。请保持文件名和相对路径与配置一致。资源内容未修改；本说明不推断外部来源。
